PORT=5000
DATABASE_URL="postgresql://user:password@host/dbname?sslmode=require"
JWT_SECRET="your_jwt_secret"
# Single origin or comma-separated origins (trailing slashes are automatically handled)
CLIENT_URL="https://smart-resto-eight.vercel.app,http://localhost:3000"
